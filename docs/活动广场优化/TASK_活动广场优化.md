# 活动广场优化任务拆分

## 任务 1：活动广场分页
- 输入契约：`displayItems` 为过滤排序后的完整列表。
- 输出契约：新增 `pagedDisplayItems`，页面每次只渲染 5 条活动。
- 验收标准：页码切换正常，筛选变化后回到第一页。

## 任务 2：往期回顾分页
- 输入契约：`pastReviewItems` 为往期回顾完整列表。
- 输出契约：新增 `pagedPastReviewItems`，页面每次只渲染 5 条回顾。
- 验收标准：页码切换正常，不影响活动广场页码。

## 任务 3：图片懒加载
- 输入契约：列表项海报和推文图继续使用 `el-image`。
- 输出契约：活动广场和往期回顾图片改为按需加载。
- 验收标准：当前页外图片不主动加载，翻页后再加载目标页图片。

## 任务 4：回归验证
- 输入契约：保留 Timeline、推荐、类别模块和报名流程。
- 输出契约：页面诊断通过，无明显功能回归。
- 验收标准：`Activities.vue` 无诊断报错，关键入口可正常点击。
