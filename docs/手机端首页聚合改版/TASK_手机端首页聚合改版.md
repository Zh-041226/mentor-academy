# 手机端首页聚合改版任务拆分

## 任务 1：移动端外壳

- 输入契约：现有 `App.vue`、`router/index.js`、桌面导航组件可用。
- 输出契约：手机端具备独立的顶部栏与底部 Tab，桌面端不受影响。
- 实现约束：不新增依赖，不破坏现有桌面端固定导航逻辑。

## 任务 2：手机端首页

- 输入契约：可复用 `HomeHeroCarousel.vue` 与活动/公告接口。
- 输出契约：新增 `MobileHome.vue`，包含 Banner、快捷入口、推荐活动、今日可报名、分类活动模块。
- 实现约束：优先显示可报名活动，卡片信息简洁，适配 375px 左右宽度。

## 任务 3：核心页面移动端适配

- 输入契约：现有 `Activities.vue`、`Announcements.vue`、`ActivityDetail.vue`、`UserCenter.vue`、`MyActivities.vue` 已可运行。
- 输出契约：上述页面在手机端具备可读布局、可点击按钮和合理间距。
- 实现约束：保持现有接口和业务逻辑不变，主要调整页面结构与样式层级。

## 任务 4：验证与收尾

- 输入契约：所有新增组件与样式已接入。
- 输出契约：构建通过，诊断无明显错误，并生成验收文档。
- 实现约束：仅修复本轮改动引入的问题，不处理无关历史问题。
