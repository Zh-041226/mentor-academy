# 公告功能验收记录

## 已完成

- 新增 Prisma `Announcement` 模型并完成数据库同步。
- 新增后台公告管理接口与公共公告接口。
- 新增公告文档上传接口，支持常见文档类型。
- 新增登录后下载公告附件接口。
- 新增前台 `公告中心` 页面。
- 新增后台 `公告管理` 页面。
- 已接入前台导航与后台菜单。
- 前端构建通过。
- 前端诊断通过。
- 后端服务启动正常，公告接口已验证可访问。

## 已验证

- `GET /api/announcements` 返回正常。
- 管理员登录后 `GET /api/admin/announcements` 返回正常。
- 后端健康检查通过。

## 待人工验证

- 后台上传 PDF/Word/Excel 等附件。
- 前台登录后下载附件。
- 公告创建、编辑、删除和发布/下线的完整页面流程。
