# 部署上线与后端聚合增强 - 接收记录

## 完成情况
- [x] 前端生产构建（vite build）完成，产物位于 `frontend/dist/`
- [x] 产物打包（`frontend/dist.zip`）完成
- [x] 后端新增聚合接口（4项）已实现并本地验证通过（200，结构符合）
- [ ] 前端产物同步至服务器（待 SSH 信息）
- [ ] Nginx 配置检查与重载（待执行）
- [ ] 线上图表与 KPI 火柴线目视验证（待执行）

## 验收检查项
1. 前端部署
   - 站点首页 200，资源无 404/500；控制台无严重错误
   - 管理仪表盘图表完整渲染：折线（累计可切换）、柱状（横/竖切换）、周-小时热力、日历热力、累计面积
2. Nginx
   - `nginx -t` 通过；`systemctl reload nginx` 成功
   - `/api/*` 正确代理并返回数据
3. 后端
   - 新增接口在生产环境返回 200 且响应结构与设计一致

## 备注
- 需要确认服务器 SSH 访问方式与站点路径是否与 `deploy/` 目录一致。确认后即可执行线上同步与验证。