# 后台活动类型扩展验收记录

## 已完成

- 后端新增历史活动类型接口 `/api/admin/activities/categories`。
- 后端创建/更新活动时统一规范化 `category` 字段。
- 前端活动类型表单改为支持预设类型、历史类型和直接输入新类型。
- 编辑活动时支持历史自定义类型回填。
- 前端诊断通过。
- 前端构建通过。
- 后端语法检查通过。

## 待人工验证

- 后台新建活动时是否能看到历史类型。
- 直接输入新类型后保存，再次打开表单时是否能重新选到。
- 编辑已有自定义类型活动时是否正常回填。
